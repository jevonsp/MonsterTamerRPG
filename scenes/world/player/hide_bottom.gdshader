shader_type canvas_item;

//uniform float mask_bottom = 0.75;
//uniform float smoothness = 0.05; // Add soft edge to prevent hard cutoff
//
//void fragment() {
    //vec4 color = texture(TEXTURE, UV);
//
    //// Add smooth transition instead of hard cutoff
    //float mask_start = 1.0 - mask_bottom;
    //float alpha_mask = smoothstep(mask_start, mask_start + smoothness, UV.y);
//
    //color.a *= (1.0 - alpha_mask);
    //COLOR = color;
//}